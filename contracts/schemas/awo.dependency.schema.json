{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AWO Dependency Declaration",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "artifact_type": {
      "type": "string",
      "description": "AWO artifact type identifier for this schema."
    },
    "run_id": {
      "type": "string",
      "description": "Stable identifier for the governed workflow run."
    },
    "awo_version": {
      "type": "string",
      "description": "Declared AWO method version used for this run (e.g. '2.0.0')."
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO8601 timestamp when this artifact was created."
    },
    "created_by": {
      "type": "string",
      "description": "Identifier for the actor (human or agent) that created this artifact."
    },
    "dependencies": {
      "type": "array",
      "description": "List of dependencies relevant to this workflow or artifact.",
      "items": {
        "type": "object",
        "required": [
          "id",
          "type"
        ],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "description": "Identifier for the dependency."
          },
          "type": {
            "type": "string",
            "description": "Type (repo, dataset, model, doctrine, spec, etc.)."
          },
          "version": {
            "type": "string",
            "description": "Version or tag if applicable."
          },
          "relation": {
            "type": "string",
            "description": "How this dependency is used (input, reference, contract, etc.)."
          }
        }
      }
    }
  },
  "required": [
    "artifact_type",
    "run_id",
    "awo_version",
    "created_at",
    "created_by",
    "dependencies"
  ]
}
