{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AWO Approval Decision",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "artifact_type": {
      "type": "string",
      "description": "AWO artifact type identifier for this schema."
    },
    "run_id": {
      "type": "string",
      "description": "Stable identifier for the governed workflow run."
    },
    "awo_version": {
      "type": "string",
      "description": "Declared AWO method version used for this run (e.g. '2.0.0')."
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO8601 timestamp when this artifact was created."
    },
    "created_by": {
      "type": "string",
      "description": "Identifier for the actor (human or agent) that created this artifact."
    },
    "approver_role": {
      "type": "string",
      "description": "Declared role of the approver (e.g., Approver, Auditor)."
    },
    "decision": {
      "type": "string",
      "description": "High-level approval decision.",
      "enum": [
        "approved",
        "rejected",
        "conditional"
      ]
    },
    "decision_scope": {
      "type": "string",
      "description": "What this decision covers (phase, run, artifact set, etc.)."
    },
    "rationale": {
      "type": "string",
      "description": "Narrative explanation supporting the decision."
    },
    "conditions": {
      "type": "array",
      "description": "If conditional, explicit conditions that must be satisfied.",
      "items": {
        "type": "string"
      }
    },
    "decided_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when the decision was made."
    }
  },
  "required": [
    "artifact_type",
    "run_id",
    "awo_version",
    "created_at",
    "created_by",
    "approver_role",
    "decision",
    "decision_scope",
    "rationale",
    "decided_at"
  ]
}
