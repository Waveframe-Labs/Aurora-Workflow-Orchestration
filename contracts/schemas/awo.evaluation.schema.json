{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AWO Evaluation Criteria",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "artifact_type": {
      "type": "string",
      "description": "AWO artifact type identifier for this schema."
    },
    "run_id": {
      "type": "string",
      "description": "Stable identifier for the governed workflow run."
    },
    "awo_version": {
      "type": "string",
      "description": "Declared AWO method version used for this run (e.g. '2.0.0')."
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO8601 timestamp when this artifact was created."
    },
    "created_by": {
      "type": "string",
      "description": "Identifier for the actor (human or agent) that created this artifact."
    },
    "criteria": {
      "type": "array",
      "description": "List of evaluation criteria that will be used to judge outcomes.",
      "items": {
        "type": "object",
        "required": [
          "id",
          "description"
        ],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "metric": {
            "type": "string",
            "description": "Optional metric or indicator."
          },
          "threshold": {
            "type": "string",
            "description": "Qualitative or quantitative threshold."
          }
        }
      }
    },
    "success_conditions": {
      "type": "string",
      "description": "Narrative description of what counts as success."
    },
    "failure_conditions": {
      "type": "string",
      "description": "Narrative description of what counts as failure."
    }
  },
  "required": [
    "artifact_type",
    "run_id",
    "awo_version",
    "created_at",
    "created_by",
    "criteria"
  ]
}
