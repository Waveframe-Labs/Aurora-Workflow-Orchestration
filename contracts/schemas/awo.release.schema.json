{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://waveframelabs.org/schemas/awo.release.schema.json",
  "title": "AWO Release Artifact",
  "description": "The terminal artifact of an AWO workflow cycle. Acts as a frozen manifest that bundles upstream artifacts for distribution or archival without asserting approval, legitimacy, or authority.",
  "type": "object",
  "required": [
    "type",
    "version",
    "status",
    "artifact_id",
    "phase",
    "created",
    "initiation_ref",
    "specification_ref",
    "mutable",
    "release_state",
    "actors",
    "release_meta",
    "manifest",
    "integrity_attestation"
  ],
  "properties": {
    "type": {
      "const": "awo.release",
      "description": "Replaces 'artifact_type' to align with ARI Metadata Policy."
    },

    "version": {
      "type": "string",
      "description": "Replaces 'awo_version' to align with ARI Metadata Policy (SemVer).",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)$"
    },

    "status": {
      "type": "string",
      "enum": ["draft", "active", "deprecated"],
      "default": "draft",
      "description": "REQUIRED by Invariant I-2. Defines the lifecycle state of the artifact file."
    },

    "artifact_id": {
      "type": "string",
      "format": "uuid",
      "description": "Globally unique identifier for this release artifact."
    },

    "phase": {
      "const": "release"
    },

    "created": {
      "type": "string",
      "format": "date-time",
      "description": "Replaces 'created_at' to align with standard YAML metadata keys."
    },

    "initiation_ref": {
      "type": "object",
      "required": ["ref_type", "identifier"],
      "properties": {
        "ref_type": {
          "type": "string",
          "enum": ["internal_id", "uri", "doi"]
        },
        "identifier": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },

    "specification_ref": {
      "type": "object",
      "required": ["ref_type", "identifier"],
      "properties": {
        "ref_type": {
          "type": "string",
          "enum": ["internal_id", "uri", "doi"]
        },
        "identifier": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },

    "mutable": {
      "const": false,
      "description": "Release artifacts are immutable once created."
    },

    "release_state": {
      "type": "string",
      "enum": ["prepared", "frozen", "withdrawn"],
      "description": "State of the release process (prepared=draft, frozen=final)."
    },

    "actors": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["actor_id", "role", "ai_assisted"],
        "properties": {
          "actor_id": { "type": "string" },
          "role": {
            "type": "string",
            "enum": ["auditor"],
            "description": "Release must be signed by an 'auditor', not a 'contributor' (Invariant I-4)."
          },
          "ai_assisted": {
            "type": "boolean"
          }
        },
        "additionalProperties": false
      }
    },

    "release_meta": {
      "type": "object",
      "required": ["version_tag", "release_intent", "target_audience"],
      "properties": {
        "version_tag": {
          "type": "string",
          "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(?:-[0-9A-Za-z.-]+)?(?:\\+[0-9A-Za-z.-]+)?$"
        },
        "release_intent": {
          "type": "string",
          "enum": ["draft_preview", "peer_context", "production_context", "archival", "deprecation"]
        },
        "target_audience": {
          "type": "string",
          "enum": ["internal", "ecosystem", "public"]
        },
        "license_declaration": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },

    "manifest": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["ref_type", "identifier", "type"],
        "properties": {
          "ref_type": {
            "type": "string",
            "enum": ["internal_id", "uri", "doi"]
          },
          "identifier": {
            "type": "string"
          },
          "type": {
            "type": "string",
            "enum": [
              "awo.initiation",
              "awo.specification",
              "awo.execution",
              "awo.review"
            ],
            "description": "Renamed from 'artifact_type' to match the property key in referenced files."
          },
          "inclusion_reason": {
            "type": "string",
            "enum": ["core_component", "supporting_evidence", "lineage_trace"]
          }
        },
        "additionalProperties": false
      }
    },

    "integrity_attestation": {
      "type": "object",
      "required": ["bundle_complete", "upstream_modified"],
      "properties": {
        "bundle_complete": {
          "type": "boolean"
        },
        "upstream_modified": {
          "const": false,
          "description": "Must be false. Verifies no upstream artifacts were altered."
        }
      },
      "additionalProperties": false
    },

    "terminal_attestation": {
      "type": "object",
      "required": ["workflow_concluded"],
      "properties": {
        "workflow_concluded": {
          "const": true
        }
      },
      "additionalProperties": false
    }
  },

  "allOf": [
    {
      "if": {
        "properties": {
          "release_state": { "const": "frozen" }
        }
      },
      "then": {
        "required": ["terminal_attestation"]
      }
    }
  ],

  "additionalProperties": false
}
