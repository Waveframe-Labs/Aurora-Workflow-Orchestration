{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AWO Contribution Artifact",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "artifact_type": {
      "type": "string",
      "description": "AWO artifact type identifier for this schema."
    },
    "run_id": {
      "type": "string",
      "description": "Stable identifier for the governed workflow run."
    },
    "awo_version": {
      "type": "string",
      "description": "Declared AWO method version used for this run (e.g. '2.0.0')."
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO8601 timestamp when this artifact was created."
    },
    "created_by": {
      "type": "string",
      "description": "Identifier for the actor (human or agent) that created this artifact."
    },
    "contribution_type": {
      "type": "string",
      "description": "Type of contribution (e.g., 'analysis', 'code', 'figure', 'dataset')."
    },
    "summary": {
      "type": "string",
      "description": "Short description of what this contribution adds to the workflow."
    },
    "inputs": {
      "type": "array",
      "description": "References to input artifacts or external resources.",
      "items": {
        "type": "string"
      }
    },
    "outputs": {
      "type": "array",
      "description": "References or identifiers for generated artifacts.",
      "items": {
        "type": "string"
      }
    },
    "notes": {
      "type": "string",
      "description": "Optional free-form notes about this contribution."
    }
  },
  "required": [
    "artifact_type",
    "run_id",
    "awo_version",
    "created_at",
    "created_by",
    "contribution_type",
    "summary"
  ]
}
