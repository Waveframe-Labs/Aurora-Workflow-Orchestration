{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AWO Initiation Record",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "artifact_type": {
      "type": "string",
      "description": "AWO artifact type identifier for this schema."
    },
    "run_id": {
      "type": "string",
      "description": "Stable identifier for the governed workflow run."
    },
    "awo_version": {
      "type": "string",
      "description": "Declared AWO method version used for this run (e.g. '2.0.0')."
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO8601 timestamp when this artifact was created."
    },
    "created_by": {
      "type": "string",
      "description": "Identifier for the actor (human or agent) that created this artifact."
    },
    "title": {
      "type": "string",
      "description": "Short title for the workflow run or research initiative."
    },
    "objective": {
      "type": "string",
      "description": "Primary research objective stated in natural language."
    },
    "motivating_context": {
      "type": "string",
      "description": "Background context or problem statement motivating the workflow."
    },
    "assumptions": {
      "type": "array",
      "description": "Explicit assumptions declared at initiation.",
      "items": {
        "type": "string"
      }
    },
    "constraints": {
      "type": "array",
      "description": "Declared hard constraints (time, resources, scope, etc.).",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "artifact_type",
    "run_id",
    "awo_version",
    "created_at",
    "created_by",
    "title",
    "objective"
  ]
}
