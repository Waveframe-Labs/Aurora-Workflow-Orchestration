{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AWO Reasoning Record",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "artifact_type": {
      "type": "string",
      "description": "AWO artifact type identifier for this schema."
    },
    "run_id": {
      "type": "string",
      "description": "Stable identifier for the governed workflow run."
    },
    "awo_version": {
      "type": "string",
      "description": "Declared AWO method version used for this run (e.g. '2.0.0')."
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO8601 timestamp when this artifact was created."
    },
    "created_by": {
      "type": "string",
      "description": "Identifier for the actor (human or agent) that created this artifact."
    },
    "context": {
      "type": "string",
      "description": "Context in which this reasoning event took place."
    },
    "inferences": {
      "type": "array",
      "description": "List of explicit reasoning steps, each auditable.",
      "items": {
        "type": "object",
        "required": [
          "id",
          "statement"
        ],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string"
          },
          "statement": {
            "type": "string",
            "description": "The claim or inference made."
          },
          "supports": {
            "type": "array",
            "description": "IDs or references of prior inferences or evidence used.",
            "items": {
              "type": "string"
            }
          },
          "source_type": {
            "type": "string",
            "description": "Human, AI, hybrid, or other origin descriptor."
          },
          "pointer": {
            "type": "string",
            "description": "Optional external pointer (e.g., log reference, prompt ID)."
          }
        }
      }
    }
  },
  "required": [
    "artifact_type",
    "run_id",
    "awo_version",
    "created_at",
    "created_by",
    "context",
    "inferences"
  ]
}
